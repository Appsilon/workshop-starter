This version of the app contains a button that toggles plots into a fullscreen mode via the `requestFullscreen` API.

Let's run the app, open up the browser devtools and discover why it's not working.
